{
  "name": "print-news-studio",
  "version": "1.1.1",
  "private": true,
  "description": "Local browser app for pasting Excel news rows, managing source logos, editing highlights, and exporting clean news layouts.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
